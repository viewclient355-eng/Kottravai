Migration Validation Report — 2026-06-08

Summary:
- Created migration: `20260608_add_lead_fields.sql` to add new lead columns, constraints, and indexes.
- Created migration: `20260608_leads_rls.sql` to recommend enabling RLS and adding safe insert policy for client inserts.
- Updated canonical schema: `server/schema.sql` to include new `leads` columns and indexes.
- Added admin JSON endpoint `/api/leads` and CSV export `/api/leads/export` to `server/index.js` for admin access.
- Updated frontend types in `src/services/leadService.ts` and added `leads` view + export/listing UI to `src/pages/admin/AdminDashboard.tsx`.

Migration Files:
- server/migrations/20260608_add_lead_fields.sql
  - Adds columns: priority (text), lead_score (integer), utm_source, utm_medium, utm_campaign, last_contacted_at (timestamptz), next_followup_at (timestamptz)
  - Adds check constraint `leads_priority_check` safely using a DO $$ block
  - Adds indexes: idx_leads_next_followup_at, idx_leads_priority, idx_leads_utm_source, idx_leads_lead_type, idx_leads_status, idx_leads_created_at
  - Idempotent: uses IF NOT EXISTS and ALTER TABLE ... ADD COLUMN IF NOT EXISTS

- server/migrations/20260608_leads_rls.sql
  - Enables RLS on `leads` and adds a recommended insert policy allowing `anon`/`authenticated` inserts requiring a non-empty `source`.
  - NOTE: Review and tighten policy before applying to production. Service role bypasses RLS.

Validation Steps Taken:
- Codebase updated to reference new fields (`lead_score`, `priority`, `utm_*`, `last_contacted_at`, `next_followup_at`) in frontend types and admin UI.
- Added server endpoints to export and fetch leads; these endpoints use `authenticateAdmin` (header `x-admin-secret`) and query the `leads` table directly.
- Added recommended RLS migration for teams that enforce RLS; left the policy permissive for client inserts but constrained by `source` presence — recommend tightening further.

Recommended Next Actions (before deploying to production):
1. Run the migration against a staging database and inspect the schema:

```bash
psql "$DATABASE_URL" -f server/migrations/20260608_add_lead_fields.sql
psql "$DATABASE_URL" -f server/migrations/20260608_leads_rls.sql
```

2. Validate the schema:

```sql
-- Connect to DB
\d+ leads
-- Verify policies
SELECT policyname, permissive, roles, qual, with_check FROM pg_policies WHERE tablename = 'leads';
```

3. If you use Supabase dashboard, mirror the same policies there and test inserts from the frontend (anon key) and via server (service role).
4. Audit RLS and ensure that sensitive columns (if any) are protected by policies granting access only to appropriate roles. Consider routing all writes through a server endpoint if you want stricter control.

Notes & Caveats:
- The RLS policy included is intentionally permissive to allow existing frontend anon-key inserts to continue working. For stricter security, prefer allowing inserts only from authenticated users or via server endpoints using the service role key.
- The admin export endpoint returns CSV and relies on `authenticateAdmin` — ensure `VITE_ADMIN_PASSWORD` is rotated and stored securely.

Files changed (quick list):
- server/migrations/20260608_add_lead_fields.sql
- server/migrations/20260608_leads_rls.sql
- server/schema.sql
- server/index.js
- src/services/leadService.ts
- src/pages/admin/AdminDashboard.tsx

Generated by: automated repo update script
Date: 2026-06-08
